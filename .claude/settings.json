{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo $CLAUDE_TOOL_INPUT | python3 -c \"import sys,json; cmd=json.load(sys.stdin).get('command',''); blocked=['rm -rf /','mkfs','dd if=','shutdown','reboot','vast destroy']; [exit(2) for b in blocked if b in cmd]\""
          }
        ]
      }
    ]
  }
}
